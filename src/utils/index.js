import { emotions, identifierData, labels, values } from './data-face';
import { convertImage64ToFileInBase64, getPercent, typeMobile } from './universal-methods';

export { emotions, identifierData, values, labels, getPercent, convertImage64ToFileInBase64, typeMobile };
